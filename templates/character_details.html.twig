<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Détails du personnage</title>
		<link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
		<style>
			body {
				background-color: #f8f9fa; 
			}

			.character-details {
				border: 1px solid #dee2e6;
				border-radius: 10px; 
				background-color: #fff; 
				box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
			}

			.character-details img {
				width: 200px;
				height: auto;
				border-radius: 50%; 
				border: 6px solid #fff; 
				box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
			}

			.character-details h1 {
				color: #343a40;
			}

			.character-details h2 {
				color: #343a40; 
			}

			.list-group-item {
				background-color: transparent; 
				border: none; 
				color: #6c757d;
			}

			.list-group-item:not(:last-child) {
				border-bottom: 1px solid #dee2e6; 
			}
		</style>
	</head>
	<body>
		<div class="container mt-5">
			<div class="card character-details">
				<div class="card-header bg-dark text-white text-center">
					<h1>Détails du personnage</h1>
				</div>
				<div class="card-body text-center">
					{% if character.data.imageUrl is defined %}
						<img src="{{ character.data.imageUrl }}" alt="{{ character.data.name }}" class="img-fluid">
					{% else %}
						<div class="alert alert-danger" role="alert">
							Image non disponible pour
							{{ character.data.name }}
						</div>
					{% endif %}
					<h2 class="mt-3">{{ character.data.name }}</h2>

					{% if films is not empty %}
						<div class="mt-4">
							<h4>Films:</h4>
							<ul class="list-group list-group-flush">
								{% for film in films %}
									<li class="list-group-item">{{ film }}</li>
								{% endfor %}
							</ul>
						</div>
					{% else %}
						<div class="mt-4">
							<p class="text-muted">Aucun film répertorié.</p>
						</div>
					{% endif %}

					{% if tvShows is not empty %}
						<div class="mt-4">
							<h4>Émissions de télévision:</h4>
							<ul class="list-group list-group-flush">
								{% for tvShow in tvShows %}
									<li class="list-group-item">{{ tvShow }}</li>
								{% endfor %}
							</ul>
						</div>
					{% else %}
						<div class="mt-4">
							<p class="text-muted">Aucune émission de télévision répertoriée.</p>
						</div>
					{% endif %}
				</div>
			</div>
		</div>
		<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
		<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
	</body>
</html>

